import React from "react";
import { Text, View } from "react-native";

const BorrowerScreen = () => {
	return (
		<View>
			<Text>It's the Borrower Screen</Text>
		</View>
	);
};

export default BorrowerScreen;
