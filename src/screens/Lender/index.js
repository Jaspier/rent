import React from "react";
import { Text, View } from "react-native";

const LenderScreen = () => {
	return (
		<View>
			<Text>It's the Lender Screen</Text>
		</View>
	);
};

export default LenderScreen;
